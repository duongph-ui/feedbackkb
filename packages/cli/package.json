{
  "name": "feedbackkb",
  "version": "0.0.0",
  "description": "FeedbackKB CLI — register | init-mcp | init-hook | init-rules | sync",
  "license": "MIT",
  "type": "module",
  "bin": { "feedbackkb": "dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.0.0"
  }
}
